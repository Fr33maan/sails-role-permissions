{
  "name": "sails-hook-role-permissions",
  "version": "1.0.0",
  "description": "A sails hook to manager role based permissions at action/row level",
  "main": "lib/index.js",
  "scripts": {
    "compile": "babel -d lib/ src/",
    "pub": "npm run compile && npm publish",
    "test": "npm run compile && mocha",
    "w": "npm run compile && mocha --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-core": "^6.21.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-0": "^6.16.0",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "sails": "^0.12.11",
    "sinon": "^1.17.7"
  }
}
